# 제이쿼리 : jquery
* 자바스크립트를 이용한 html 동적처리를 좀 더 쉽게 해주는 라이브러리 언어 제이쿼리
*`<title></title>` 태그 다음 위치에 **CDN 주소 연결 필수** https://cdnjs.com/libraries/jquery
## 제이쿼리 기본 문법
* `$()` 제이쿼리 기본 선언법
* `$('.클래스명')`
* `$('#아이디명')`
* `$('태그명')`
* `''` 따옴표 안 CSS 선택자처럼 원하는 대상 작성가능
* `$('대상').show();` 대상처럼 보이기
* `$('대상').hide();` 대상 숨기기
* `$('대상').css('속성','값');` CSS 적용하기
* `$('대상').removeClass();` class 전부 제거하기
 * `$('대상').removeClass('추가클래스명');` 해당 클래스명 제거하기
* `$('대상').addClass('추가클래스명');` class 적용하기
# 자바스크립트 260306
* var data_create;
* 선언키워드 변수명
* data_create=3;
* 변수명 대입연산자(할당연산자,오른쪽 값을 왼쪽에 넣는다(대입한다)) 변수값
## 변수
* 프로그래밍의 첫번째 시작작업은 반드시 필요한 데이터들을 **변수(저장소)**로 저장해야 한다
## 변수작성법
* `변수선언키워드 생성변수명;` -> 빈 저장소 생성
 * `ver box;`
* `변수선언키워드 생성변수명 대입연산자 대입값;`
 * `ver box = 1;`
* `생성변수명 대입연산자 대입값;`
 * `box = 2;`
**변수 ver** : 오래된...잘안씀  **변수 let** : 값을 바꿀수있음 (변경o) **상수 const** : 값이바뀔수없음 (변경x)
* 변수선언키워드 : ver, let, const
* 생성변수명 - 영문, 숫자조합(숫자는 영문뒤로)
## 변수작성시 주의사항
* 변수의 이름은 문자(a~z,A~Z), 숫자(0~9), 달러($), 밑줄(_)이 포함됩니다
* 변수의 이름은 숫자로 시작할 수 없습니다
* 변수의 이름은 대시(-), 마침표(.) 기호를 사용할 수 없습니다
* 키워드나 예약어는 사용할 수 없습니다
* 키워드, 예약어 종류 : delete, this, false, true, if, defauit, var, new, class 등 
* 모든 변수는 대/소문자를 구별합니다 score와 Score는 다른 변수입니다
* 변수가 저장할 정보의 종류를 표현하는 의미있는 단어를 사용해야 합니다
## 변수 표기법 종류
* 캐멀 표기법 : newName / webSiteCode
* 파스칼 표기법 :  NewName / WebSiteCode
* 밑줄 표기법 : new_name / web_site_code
## 객체, 속성, 메서드 작성법
* `객체;` >> 객체읽음
* `객체.속성;` >> 객체의 속성 읽음
* `객체.메서드();` >> 객체를 메서드 방법으로 실행
* `객체.속성.메서드();`
* `객체.속성.속성.메서드();`
* `객체.메서드().속성;`
* `객체.메서드().메서디();`
* `객체.메서트(메서드());`
### 자바스크립트는 동적언어로 변수 정의 시 자료형 정의를 할 필요가 없고, 대입되는 데이터 종류에 따라 변수의 데이터 종류가 결정됩니다
* 자료형 종류 : 원시형/숫자형/문자형/불리언형/Undefined형/오브젝트형
## 데이터타입 구분 함수 'typeof()'
* CSS의 배경색상으로 영역구분하는것처럼 데이터타입 구분목적으로 사용
1. 특정 데이터타입을 담아둔 변수를 생성
 * `let num = 1;`
 * `let str = '1';`
2. 해당 변수의 데이터타입을 구분하기 위해 typeof 함수확인
 * `consol.log(typeof(num));` // Numbrber(숫자)
 * `consol.log(typeof(str));` // String(문자)
* typeof = 함수 (${typeof }) OR (${typeof ()}) 모두 가능
* **숫자와 문자(글)의 차이** 1+1 = 2 `Numbrber(숫자)` / 1+1 = 11 `String(글)`
* `=` 대입연산자가 있으면 오른쪽부터 읽기
* `+` 문자열 연결 연산자 `,(콤마)` 구분자 역할
## null 과 Undefined 차이
* null : 값이 비어있음, 존재하지 않음, 사용자에 의해 결정되는 의도적인 대입 값
* Undefined : 정의(할당)되지 않음, 변수를 초기화하지 않은 오류상태, Undefined를 변수에 대입해서는 안됨, 사용자가 실수로 초기화하지 않은 변수에 들어간 일종의 오류값
## 표현식과 연산자
* `x : 단항 연산자`
* `x + y : 이항 연산자`
* `조건 x + y : 삼항 연산자`
* 연결연산자 인식 = 문자열 + 문자열 / 문자열 + 숫자
* 숫자 + 숫자 = 더하기 연산자 인식
* (숫자 + 숫자) + 문자열 = 더하기 처리후 최종적으로 문자열로 결합
## 산술연산자
* 피연산자가 숫자인경우 >> 산술연산자
* `a + b` 더하기 | 두개의 피연산자를 더한다
* `a - b` 뺴기 | 왼쪽의 피연산자에서 오른쪽 피연산자를 뺸다
* **`a * b`** 곱하기 | 두개의 피연산자를 곱한다
* **`a / b`** 나누기 | 왼쪽의 피연산자를 오른쪽 피연산자로 나눈 몫이다
* **`a % b`** 나머지 | 왼쪽의 피연산자를 오른쪽 피연산자로 나눈  나머지이다
* `a ** b` 거듭제곱 | 왼쪽의 피연산자의 오른쪽 피연산자의 값만큼 곱한 값이다
* 계산할 수 없는 경우는  NaN 처리 (ex) 'two' * 2 = NaN
* 더하기 연산자는 피연산자에 따라 문자열이 하나라도 있으면 문자열 결합으로 나타나지만, 뺴기,곱하기,나누기 연산자는 숫자형으로 자동 형변환하여 인식함 
* (ex) '일'+ 1 = 연결연산자/문자열결합 / '1' + 1 = 숫자형으로 형변환, 2